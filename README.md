# User Management System

A simple browser-based user management application with automatic ID generation and sorting functionality.

## Quick Start
1. Create the file structure as described
2. Open `index.html` using IntelliJ's built-in server (right-click → Open in Browser)
3. **Important:** Must run via localhost (not file://) for localStorage to work

*Data persists between sessions via browser localStorage*
